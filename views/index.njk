{% extends "layout.njk" %}}

{% block content %}
<div class="content">

    <div class="ad">
        <div class="text-box">
            <h1>As melhores receitas</h1>
            <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
        </div>
        <img src="/assets/chef.png" alt="Ilustração de um chef de cozinha">
    </div>

    <section class="samples">
        <h2>Mais acessadas</h2>

        <div class="cards">
            {% for recipe in recipes %}
            <div class="card" id="{{recipe.id}}"> {# AQUI EU TIREI recipe.image #}
                <img src="{{recipe.image}}" alt="Imagem {{recipe.description}}">
                <p class="image-description">{{recipe.title}}</p>
                <p class="references">Por {{recipe.author}}</p>
            </div>
            {% endfor %}
            
        </div>
    </section>


    {# <!-- MODAL -->
    <div class="modal-background">
        <div class="modal">
            <img class="modalImage" src="" alt="">
            <div class="modal-text">
                
            </div>
            
            <div class="close-button">
                <p>fechar</p>
            </div>
            
        </div>

    </div> #}

</div>
{% endblock content %}

{% block scripts %}
<script src="/scripts/scripts.js"></script>
{% endblock scripts %}

