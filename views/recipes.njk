{% extends "layout.njk" %}

{% block content %}
<div class="content">

    <section class="samples">

        <div class="cards">
            {% for recipe in recipes %}
            <div class="card" id="{{recipe.image}}">
                <img src="{{recipe.image}}" alt="Imagem {{recipe.description}}">
                <p class="image-description">{{recipe.title}}</p>
                <p class="references">{{recipe.author}}</p>
            </div>
            {% endfor %}
            {% for recipe in recipes %}
            <div class="card" id="{{recipe.image}}">
                <img src="{{recipe.image}}" alt="Imagem {{recipe.description}}">
                <p class="image-description">{{recipe.title}}</p>
                <p class="references">{{recipe.author}}</p>
            </div>
            {% endfor %}
            
        </div>

    </section>

</div>

<!-- MODAL -->
<div class="modal-background">
    <div class="modal">
        <img class="modalImage" src="" alt="">
        <div class="modal-text"></div>
        <div class="close-button">
            <p>fechar</p>
        </div>

    </div>

</div>
{% endblock content %}

{% block scripts %}
    <script src="/scripts/scripts.js"></script>
{% endblock scripts %}